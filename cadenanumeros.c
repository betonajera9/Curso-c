#include <stdio.h>
#include <stdlib.h>

int IsInteger(int a){

}

int main(){
  int x, a, b, c;
  puts("De cuantos valores es tu cadena?");
  scanf("%d",&x);
  
  scanf("%d%d%d",&a, &b, &c);
  if(a < 0)
    printf("%d",a);
  else
  printf("hola");

  return EXIT_SUCCESS;
}
